import React from "react";
import { Grid, Typography } from "@material-ui/core";

const SearchHeader = () => {
  return (
    <Grid item xs={12}>
      <Typography variant="h2">DeepDive</Typography>
    </Grid>
  );
};

export default SearchHeader;
